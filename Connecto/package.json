{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "main": "electron.js",
  "scripts": {
    "build": "nuxt build",
    "start": "nuxt start",
    "dev": "nuxt dev",
    "electron": "electron electron.cjs --no-sandbox .",
    "electron:dev": "nuxt dev & wait-on http://localhost:3000 && electron electron.cjs",    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.13.2",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "electron": "^33.0.2"
  }
}
